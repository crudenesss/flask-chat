# Non privileged user credentials (to execute commands with)
POSTGRES_USERNAME=user
POSTGRES_PASSWORD=password

# Postgres misc properties
POSTGRES_HOSTNAME=postgresql
PGDATABASE=chat_db

# Logging configuration (Preferred not to be changed on prod)
LOGGING_LEVEL=INFO

# Nginx
CHAIN_PATH=/path/to/fullchain/cert.pem
PRIVKEY_PATH=/path/to/privkey.pem
DOMAIN_NAME=example.com
